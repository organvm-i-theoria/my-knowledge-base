node_modules/
dist/
*.log
.env
.DS_Store

# Database files (keep schema, ignore data)
db/*.db
db/*.db-shm
db/*.db-wal

# Raw exports (too large for git)
raw/

# Embeddings (binary data)
atomized/embeddings/chroma/
sync/

# Sensitive intake files (passwords, tokens, API keys)
intake/**/user_profile/
intake/**/Login*/
intake/**/*PW.md
intake/**/*.webarchive
intake/**/pieces-desktop-*/

# Raw imported data (too large, many false positives in secret scanner)
intake/omni-root-import/

# Large ChatGPT export files (exceed GitHub 100MB limit)
intake/**/conversations.json
intake/**/chat.html

# Copilot activity exports
intake/*.csv
intake/*.zip
intake/_Archives/
intake/data-*/
intake/ttl/

# Playwright browser sessions (contains auth cookies)
.playwright/

# Debug scripts
src/debug-*.ts
